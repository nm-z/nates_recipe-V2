---
description: 
globs: 
alwaysApply: true
---
PR REVIEW PROCESS
- Must continue checking for new PRs until `gh pr list` shows "no open pull requests"
- Cannot stop PR review process until verified no PRs left open
- New PRs may come in while processing existing ones
- Use `env GH_PAGER=cat gh pr list` to avoid shell configuration issues

After PR reviews, ALWAYS clean up branches:
- Local: `git branch | grep -v "main\|master" | xargs -r git branch -D`
- Remote: `git branch -r | grep -v "origin/main\|origin/master\|origin/HEAD" | sed 's/origin\///' | xargs -I {} git push origin --delete {}`
- Cleanup: `git remote prune origin`
- Repository should only have main (WIP) and master (stable) branches

IMPORTANT COMMANDS
==================
- PR List: `env GH_PAGER=cat gh pr list`
- PR View: `env GH_PAGER=cat gh pr view <ID>`
- PR Merge: `gh pr merge <ID> --squash`
- PR Close: `gh pr close <ID> --comment "reason"`
- Branch Cleanup: See "BRANCH CLEANUP MEMORY" section above
- Environment Test: `python test_imports.py`
- Basic Training Test: `python orchestrator.py --data DataSets/1/D1-Predictors.csv --target DataSets/1/D1-Targets.csv --time 60 --autogluon`
